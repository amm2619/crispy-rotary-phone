{
  "name": "records-server",
  "version": "1.0.0",
  "description": "Optional local server for Records timekeeping app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
